import { BigInt, Bytes, ethereum } from '@graphprotocol/graph-ts';

// TODO: replace these handlers with generated contract bindings and entity persistence.

export function handleSwap(event: ethereum.Event): void {
  event;
}

export function handleLiquidityAdded(event: ethereum.Event): void {
  event;
}

export function handleLiquidityRemoved(event: ethereum.Event): void {
  event;
}

export function handleVaultDeposit(event: ethereum.Event): void {
  event;
}

export function handleProposalCreated(event: ethereum.Event): void {
  event;
}

export function handleVoteCast(event: ethereum.Event): void {
  event;
}
